---
- name: Configure docker
  notify:
    - 'Enable docker'
  block:
    - name: Install docker
      ansible.builtin.dnf:
        name: 'docker'
        state: present

    - name: Allow melvyn to run docker
      ansible.builtin.user:
        name: melvyn
        groups: docker
        append: true
